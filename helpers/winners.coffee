rfr = require('rfr')
secrets = rfr('./secrets.json')

module.exports = {

	# TO BE POPULATED AT THE END OF THE TRIAL

	# Example format:
	# 'user-id': {
	#     secret: sha256('user-id' + secrets['winner_salt'])
	#     prize: 'prize-code'
	# }

}
